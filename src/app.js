import './src/app.html';


function wheel() {
    /* When adding and removing items
    use the math function [100% / num of items]
    to have each segment be equal*/
}

function wheelSpin() {
    
}

function serializeList() {
    /*when an item is added, you need to push the item to a json to be pulled back later */
}

function deserializeList() {
    /* when an item is removed, it needs to be removed from the json file */
}